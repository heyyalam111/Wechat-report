# Chatlog Analyzer 执行提示词

## 任务

批量分析微信群聊记录并生成精美HTML报告。

## 执行步骤

### 1. 检查环境
- 检查 `群聊清单.md` 是否存在
- 检查数据库文件是否存在（默认路径：`MSG0_decrypted.db/de_MSG0.db`）
- 可选：检查 `chatrooms.csv` 是否存在（用于群名到群ID的映射）

### 2. 解析配置
读取 `群聊清单.md` 获取要分析的群聊列表和日期。

支持的日期格式：
- `今天` - 自动转换为当前日期
- `昨天` - 自动转换为前一天
- `2026-02-09` - 具体日期

### 3. 获取消息
两种数据源（按优先级）：
1. **chatlog HTTP API** - 需要先启动 chatlog 服务
2. **本地数据库** - 解密后的微信数据库

### 4. 分析话题
- 按30分钟时间窗口分组消息
- 计算每个话题的价值分数
- 选择得分最高的3个话题

评分算法：
```
score = 消息数×1.0 + (字符数/100)×0.5 + 参与人数×2.0 + 关键词数×1.5
```

### 5. 生成报告
输出现代化深色主题HTML报告，包含：
- 统计卡片（总消息数、话题数、参与人数）
- 热门话题列表（标题、摘要、关键词、精选消息）

### 6. 输出结果
报告保存到 `chatlog_reports_YYYYMMDD/` 目录。

## 常见问题

### Q: 找不到数据库
A: 使用 `--db` 参数指定数据库路径

### Q: 找不到群聊
A: 检查 `chatrooms.csv` 获取准确的群聊名称

### Q: 没有消息
A: 确认日期正确，或使用 `--auto-date` 自动获取最新日期
